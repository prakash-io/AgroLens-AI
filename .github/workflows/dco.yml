name: DCO Check

on:
  pull_request:
  push:

jobs:
  dco:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: DCO Check
        uses: Union-Community/dco-action@v1
